# Lerna monorepo — 環境變數範本
# -----------------------------------------------------------------------------
# 用法：複製為 .env 或 .env.local，填入實際值。檔案被 .gitignore 排除。
# CI 環境用 GitHub Actions secrets 設定同名變數。
# -----------------------------------------------------------------------------

# === Supabase（apps/web、apps/mobile build 時注入到 HTML） ====================
# 來源：Supabase Dashboard > Project Settings > API
# 注意：anon key 是 public，可進前端 bundle；service_role key 絕對不要放這裡。
LERNA_SUPABASE_URL=
LERNA_SUPABASE_ANON_KEY=

# === services/api（W2-3 起用，目前 stub） ===================================
# 待選型完成後補：DATABASE_URL、JWT_SECRET、JWT_REFRESH_SECRET、
# RATE_LIMIT_WINDOW_MS、RATE_LIMIT_MAX、SUPABASE_SERVICE_ROLE_KEY 等
# DATABASE_URL=
# JWT_SECRET=
# JWT_REFRESH_SECRET=

# === Sentry / PostHog（W10 起用） ===========================================
# SENTRY_DSN=
# POSTHOG_KEY=
# POSTHOG_HOST=

# === Stripe（W11 起用） =====================================================
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
